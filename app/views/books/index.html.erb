<p id="notice"><%= notice %></p>

<h1>Books</h1>

<div class='row'>
  <div class='col-sm-12'>
    <p>
      <form>
        <legend>Search</legend>
        <div class='col-xs-4'>
          <input type='text' class='form-control' value='<%= params[:keyword] %>' name='keyword' placeholder='Keyword' >
        </div>
      </form>
    </p>
    <p class='pull-right'>
      <a href='#' id='show-form' class='btn btn-primary'>New Book Form</a>
    </p>
  </div>
</div>


<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Title</th>
      <th>Author</th>
      <th>Description</th>
      <th>Amazon</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr class="<%= 'finished' if book.finished? %>">
        <td>
          <a href="#" target="_blank"><%= image_from_amazon(book.amazon_id) %></a>
        </td>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.description %></td>
        <td><%= book.amazon_id %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
